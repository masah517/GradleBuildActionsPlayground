name: Gradle Build Action CI

on:
  pull_request:
    paths-ignore:
      - '**.md'
jobs:
  buildDevelopApk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          distribution: 'zulu'
          java-version: '11'
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2.2.2
        with:
          cache-read-only: ${{ github.ref != 'refs/heads/release/develop' }}
      - name: Build with Gradle
        run: ./gradlew assembleDebug
